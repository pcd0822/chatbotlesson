# 로컬 개발용 예시 (netlify dev에서 사용)
# Netlify 대시보드에 등록할 실제 값은 여기에 적지 마세요!

# OpenAI API 키 (https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...

# Google 서비스 계정 JSON 전체를 한 줄로 (개행 없이)
# 또는 base64로 인코딩한 값
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
